get:
  tags:
    - Page
  summary: 'Create a page form.'
  operationId: create_page
  responses:
    200:
      description: 'The create page information.'
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: ../../models/page.yml
              meta:
                type: object
    401:
      description: Unauthorized
    403:
      description: Forbidden
